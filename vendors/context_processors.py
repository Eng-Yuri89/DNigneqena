from accounts.models import User
from core.models import Setting
from core.views.setting_views import setting

''''
def user_profile(request ):
    return {'vendor': User.objects.filter(id=request.user.id)

            }
            '''
